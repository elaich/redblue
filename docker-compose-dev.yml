version: '3'

services:
  voting-client:
    ports:
      - "3000:3000"
    volumes:
      - ./votes/client:/app
      - /app/node_modules
  voting-server:
    ports:
      - "9229:9229"
    volumes:
      - ./votes/server:/app

