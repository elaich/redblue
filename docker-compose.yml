version: '3'

services:
  voting-client:
    build: ./votes/client
    ports:
      - "3000:3000"
    volumes:
      - ./votes/client:/app
      - /app/node_modules
  voting-server:
    build: ./votes/server
    ports:
      - "5000:5000"
    volumes:
      - ./votes/server:/app
